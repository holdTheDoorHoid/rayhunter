diff --git a/CORE/CLD_TXRX/HTT/htt_rx.c b/CORE/CLD_TXRX/HTT/htt_rx.c
index 69afa95b1..d41d0120a 100644
--- a/CORE/CLD_TXRX/HTT/htt_rx.c
+++ b/CORE/CLD_TXRX/HTT/htt_rx.c
@@ -1869,6 +1869,11 @@ htt_rx_mon_amsdu_pop_hl(
 	uint8_t phymode;
 	a_bool_t ret;
 
+	{
+		static unsigned int rhmon_pop;
+		if (rhmon_pop++ < 5)
+			pr_err("RHMON: mon_amsdu_pop_hl #%u\n", rhmon_pop);
+	}
 	pdev->rx_desc_size_hl =
 		(adf_nbuf_data(rx_ind_msg))
 		[HTT_ENDIAN_BYTE_IDX_SWAP(
diff --git a/CORE/CLD_TXRX/HTT/htt_t2h.c b/CORE/CLD_TXRX/HTT/htt_t2h.c
index 9f8ba8f59..40ff9fb46 100644
--- a/CORE/CLD_TXRX/HTT/htt_t2h.c
+++ b/CORE/CLD_TXRX/HTT/htt_t2h.c
@@ -618,6 +618,13 @@ if (adf_os_unlikely(pdev->rx_ring.rx_reset)) {
                  * TODO: remove copy after stopping reuse skb on HIF layer
                  * because SDIO HIF may reuse skb before upper layer release it
                  */
+                {
+                    static unsigned int rhmon_ind;
+                    if (rhmon_ind++ < 5)
+                        pr_err("RHMON: hl rx_ind #%u peer=%d tid=%d ranges=%d conparam=%d\n",
+                               rhmon_ind, peer_id, tid, num_mpdu_ranges,
+                               (int)vos_get_conparam());
+                }
                 if (VOS_MONITOR_MODE == vos_get_conparam())
                     ol_rx_mon_indication_handler(
                             pdev->txrx_pdev, htt_t2h_msg, peer_id, tid,
diff --git a/CORE/CLD_TXRX/TXRX/ol_txrx.c b/CORE/CLD_TXRX/TXRX/ol_txrx.c
index 96d4043d5..95d04993e 100644
--- a/CORE/CLD_TXRX/TXRX/ol_txrx.c
+++ b/CORE/CLD_TXRX/TXRX/ol_txrx.c
@@ -1450,6 +1450,7 @@ ol_txrx_peer_attach(
     #endif
     if ((VOS_MONITOR_MODE == vos_get_conparam()) && !pdev->self_peer) {
         pdev->self_peer = peer;
+        pr_err("RHMON: self_peer set for monitor\n");
         /*
          * No Tx in monitor mode, otherwise results in target assert.
          * Setting disable_intrabss_fwd to true
diff --git a/CORE/HDD/src/wlan_hdd_main.c b/CORE/HDD/src/wlan_hdd_main.c
index bbf82b0a4..f86fcb354 100644
--- a/CORE/HDD/src/wlan_hdd_main.c
+++ b/CORE/HDD/src/wlan_hdd_main.c
@@ -9249,6 +9249,7 @@ static int hdd_open(struct net_device *dev)
  */
 static int __hdd_mon_open(struct net_device *dev)
 {
+	pr_err("RHMON: __hdd_mon_open entered\n");
 	hdd_adapter_t *adapter = WLAN_HDD_GET_PRIV_PTR(dev);
 	hdd_context_t *hdd_ctx =  WLAN_HDD_GET_CTX(adapter);
 	int ret;
@@ -9267,6 +9268,7 @@ static int __hdd_mon_open(struct net_device *dev)
 	vos_status = WLANTL_RegisterSTAClient(hdd_ctx->pvosContext,
 					      hdd_mon_rx_packet_cbk,
 					      &sta_desc, 0);
+	pr_err("RHMON: WLANTL_RegisterSTAClient -> %d\n", vos_status);
 	if (VOS_STATUS_SUCCESS != vos_status) {
 		hddLog(LOGE,
 		       FL("WLANTL_RegisterSTAClient() failed to register. Status= %d [0x%08X]"),
@@ -9276,6 +9278,7 @@ static int __hdd_mon_open(struct net_device *dev)
 
 	hal_status = sme_create_mon_session(hdd_ctx->hHal,
 				     adapter->macAddressCurrent.bytes);
+	pr_err("RHMON: sme_create_mon_session -> %d\n", hal_status);
 	if (eHAL_STATUS_SUCCESS != hal_status) {
 		hddLog(LOGE,
 		       FL("sme_create_mon_session() failed to register. Status= %d [0x%08X]"),
diff --git a/CORE/SERVICES/WMA/wma.c b/CORE/SERVICES/WMA/wma.c
index aaa20d729..04e88e2a0 100644
--- a/CORE/SERVICES/WMA/wma.c
+++ b/CORE/SERVICES/WMA/wma.c
@@ -1154,6 +1154,12 @@ send_fail_resp:
 	wma_send_msg(wma, WDA_ADD_BSS_RSP, (void *)add_bss, 0);
 }
 
+/* Debug: override the vdev type used at attach, to establish whether the
+ * firmware answers VDEV_START for a MONITOR-type vdev at all.
+ * 0/unset = leave the driver's own choice alone. */
+static int rhmon_vdev_type;
+module_param(rhmon_vdev_type, int, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
+
 static int wma_vdev_start_resp_handler(void *handle, u_int8_t *cmd_param_info,
 				       u_int32_t len)
 {
@@ -1178,6 +1184,8 @@ static int wma_vdev_start_resp_handler(void *handle, u_int8_t *cmd_param_info,
 		return -EINVAL;
 	}
 
+	pr_err("RHMON: VDEV_START_RESP vdev=%d status=%d\n",
+	       param_buf->fixed_param->vdev_id, param_buf->fixed_param->status);
 	resp_event = param_buf->fixed_param;
 	buf = vos_mem_malloc(sizeof(wmi_vdev_start_response_event_fixed_param));
 	if (!buf) {
@@ -10209,6 +10217,10 @@ static ol_txrx_vdev_handle wma_vdev_attach(tp_wma_handle wma_handle,
 
 	if (VOS_MONITOR_MODE == vos_get_conparam())
 		self_sta_req->type = WMI_VDEV_TYPE_MONITOR;
+	if (rhmon_vdev_type > 0)
+		self_sta_req->type = rhmon_vdev_type;
+	pr_err("RHMON: vdev_attach id=%d type=%d subtype=%d\n",
+		self_sta_req->sessionId, self_sta_req->type, self_sta_req->subType);
 
 	/* Create a vdev in target */
 	if (wma_unified_vdev_create_send(wma_handle->wmi_handle,
@@ -14970,6 +14982,9 @@ static void wma_set_channel(tp_wma_handle wma, tpSwitchChannelParams params)
 		}
 	}
 
+	pr_err("RHMON: wma_set_channel vdev=%d chan=%d vdev_up=%d conparam=%d\n",
+	       req.vdev_id, req.chan, (int)wma_is_vdev_up(0),
+	       (int)vos_get_conparam());
 	if ((VOS_MONITOR_MODE == vos_get_conparam()) && wma_is_vdev_up(0)) {
 		status = wma_switch_channel(wma, &req);
 		if (status != VOS_STATUS_SUCCESS)
@@ -14978,6 +14993,8 @@ static void wma_set_channel(tp_wma_handle wma, tpSwitchChannelParams params)
 		return;
 	} else {
 
+		pr_err("RHMON: calling wma_vdev_start vdev=%d chan=%d\n",
+		       req.vdev_id, req.chan);
 		status = wma_vdev_start(wma, &req,
 				wma->interfaces[req.vdev_id].is_channel_switch);
 		if (status != VOS_STATUS_SUCCESS) {
